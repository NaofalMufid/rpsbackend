<%- include('../partials/header') -%>
<%- include('../partials/sidebar') -%>
<!-- content -->
    <main role="main" class="col-md-9 ml-sm-auto col-lg-10 pt-3 px-4">
    <h1 class="h2">Data User Game Biodata</h1>
    <a href="/new/userbiodata" class="btn btn-primary btn-smal mb-2"><span data-feather="plus-square"></span> Add User</a>
    <div class="table-responsive">
        <table class="table table-striped table-bordered">
        <thead>
            <tr>
            <th>#</th>
            <th>Username</th>
            <th>Firstname</th>
            <th>Lasttname</th>
            <th>Gender</th>
            <th>Avatar</th>
            <th>Action</th>
            </tr>
        </thead>
        <tbody>
            <% data.forEach(i => { %>
            <tr>
                <td><%= i.id %></td>
                <td><%= i.user_id %></td>
                <td><%= i.firstname %></td>
                <td><%= i.lastname %></td>
                <td><%= i.gender %></td>
                <td><img src="<%= i.avatar %>" alt="My Avatar" width="50px"></td>
                <td class="form-inline">
                    <a href="userbiodata/<%= i.id %>" class="btn btn-warning btn-sm mr-2">
                        <span data-feather="edit"></span>
                    </a>

                    <form action="/userbiodata/<%= i.id %>/delete" method="POST">
                        <button class="btn btn-danger btn-sm" onclick="return confirm('Are you sure you want to delete this record?')">
                            <span data-feather="trash"></span>
                        </button>
                        <input type="hidden" name="_method" value="DELETE">
                    </form>
                </td>
            </tr>
            <% }) %>
        </tbody>
        </table>
    </div>
</main>
<%- include('../partials/footer') -%>